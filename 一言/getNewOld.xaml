<Page
    x:Class="一言.getNewOld"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:一言"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <SlideNavigationTransitionInfo/>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Grid>
        <WebView x:Name="w"/>
        <StackPanel x:Name="mg" Visibility="Collapsed">
            <ProgressBar IsIndeterminate="True" BorderBrush="#FF16A0E1" Width="400"/>
            <TextBlock x:Name="mt" Text="正在加载..." FontSize="18" Foreground="#FF16A0E1" HorizontalAlignment="Right" />
        </StackPanel>
    </Grid>
</Page>
